% !TEX program = xelatex
\documentclass[11pt,a4paper]{article}

\usepackage{fontspec}
\defaultfontfeatures{Ligatures=TeX,Scale=MatchLowercase}
\IfFontExistsTF{Times New Roman}{
  \setmainfont{Times New Roman}
}{
  \setmainfont{DejaVu Serif}
}
\IfFontExistsTF{Arial}{
  \setsansfont{Arial}
}{
  \setsansfont{DejaVu Sans}
}
\setmonofont{DejaVu Sans Mono}[Scale=0.82]

\usepackage[left=2cm,right=2cm,top=2cm,bottom=2cm,footskip=0.9cm]{geometry}
\usepackage{setspace}
\setstretch{1.15}
\usepackage{indentfirst}
\setlength{\parindent}{0cm}
\setlength{\parskip}{4.5pt}

\usepackage[table]{xcolor}
% Bảng màu chuyên nghiệp & hiện đại
\definecolor{PrimaryNavy}{HTML}{0F172A}
\definecolor{AccentBlue}{HTML}{1D4ED8}
\definecolor{SoftBlueBg}{HTML}{F8FAFC}
\definecolor{BorderBlue}{HTML}{CBD5E1}
\definecolor{TerminalBg}{HTML}{0F172A}
\definecolor{TerminalBorder}{HTML}{334155}
\definecolor{TerminalText}{HTML}{F8FAFC}
\definecolor{TerminalGreen}{HTML}{22C55E}
\definecolor{TerminalPrompt}{HTML}{38BDF8}
\definecolor{NoteBg}{HTML}{F0FDF4}
\definecolor{NoteBorder}{HTML}{86EFAC}
\definecolor{NoteText}{HTML}{166534}
\definecolor{WarnBg}{HTML}{FEF2F2}
\definecolor{WarnBorder}{HTML}{FECACA}
\definecolor{WarnText}{HTML}{991B1B}
\definecolor{TipBg}{HTML}{FFFBEB}
\definecolor{TipBorder}{HTML}{FDE68A}
\definecolor{TipText}{HTML}{92400E}
\definecolor{TableHeader}{HTML}{1E293B}
\definecolor{TableSubHeader}{HTML}{F8FAFC}
\definecolor{InlineCodeBg}{HTML}{F1F5F9}

\usepackage{graphicx}
\usepackage{tabularx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{listings}
\usepackage[most]{tcolorbox}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{microtype}
\usepackage{hyperref}

\hypersetup{
  colorlinks=true,
  linkcolor=AccentBlue,
  urlcolor=AccentBlue,
  pdftitle={Hướng dẫn cài đặt và chạy dự án StayGo trên Windows},
  pdfauthor={Hệ thống Đặt phòng Khách sạn StayGo},
  pdfsubject={Hướng dẫn cài đặt từng bước cho người mới bắt đầu},
  pdfkeywords={Windows, Laravel 12, Vue 3, MySQL, MongoDB, Redis, StayGo}
}

% Cấu hình Header và Footer (chỉ để lại số trang ở footer)
\pagestyle{fancy}
\fancyhf{}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0pt}
\renewcommand{\footrulewidth}{0pt}

% Định dạng tiêu đề các mục
\titleformat{\section}
  {\bfseries\color{PrimaryNavy}\fontsize{13}{16}\selectfont}
  {\thesection.}{0.5em}{}
\titlespacing*{\section}{0pt}{9pt}{3pt}

\titleformat{\subsection}
  {\bfseries\color{AccentBlue}\fontsize{10.5}{13}\selectfont}
  {\thesubsection}{0.5em}{}
\titlespacing*{\subsection}{0pt}{6pt}{2pt}

% Cấu hình TColorBox
% 1. Hộp lệnh Terminal đẹp mắt (Dành riêng để Copy)
\newtcolorbox{terminalcmd}[2][]{
  enhanced,
  breakable,
  colback=TerminalBg,
  colframe=TerminalBorder,
  arc=2mm,
  boxrule=0.7pt,
  left=3.5mm,right=3.5mm,top=2mm,bottom=2mm,
  fonttitle=\sffamily\bfseries\small, title={\textcolor{white}{[LỆNH CHẠY] -- }\textcolor{TerminalPrompt}{#2}},
  coltitle=white,
  attach boxed title to top left={yshift=-2mm,xshift=3mm},
  boxed title style={boxrule=0pt,colframe=AccentBlue,colback=AccentBlue,arc=1mm},
  fontupper=\ttfamily\color{TerminalText}\small,
  #1
}

% 2. Hộp từng bước (Step Box)
\newtcolorbox{stepbox}[2][]{
  enhanced,
  breakable,
  colback=SoftBlueBg,
  colframe=BorderBlue,
  arc=2mm,
  boxrule=0.7pt,
  left=3.5mm,right=3.5mm,top=2mm,bottom=2mm,
  title={\bfseries\color{PrimaryNavy}#2},
  coltitle=PrimaryNavy,
  attach boxed title to top left={yshift=-2mm,xshift=3mm},
  boxed title style={boxrule=0.5pt,colframe=BorderBlue,colback=white,arc=1mm},
  #1
}

% 3. Hộp Lưu ý (Note Box)
\newtcolorbox{notebox}[1][LƯU Ý QUAN TRỌNG]{
  enhanced,
  breakable,
  colback=NoteBg,
  colframe=NoteBorder,
  arc=2mm,
  boxrule=0.7pt,
  left=3mm,right=3mm,top=1.5mm,bottom=1.5mm,
  title={\bfseries\color{NoteText}[GHI CHÚ] #1},
  coltitle=NoteText,
  fontupper=\color{NoteText}\small
}

% 4. Hộp Cảnh báo / Khắc phục lỗi (Warning Box)
\newtcolorbox{warningbox}[1][CẢNH BÁO / KHẮC PHỤC SỰ CỐ]{
  enhanced,
  breakable,
  colback=WarnBg,
  colframe=WarnBorder,
  arc=2mm,
  boxrule=0.7pt,
  left=3mm,right=3mm,top=1.5mm,bottom=1.5mm,
  title={\bfseries\color{WarnText}[CẢNH BÁO] #1},
  coltitle=WarnText,
  fontupper=\color{WarnText}\small
}

% 5. Hộp Mẹo thực hành (Tip Box)
\newtcolorbox{tipbox}[1][MẸO THỰC HÀNH NHANH]{
  enhanced,
  breakable,
  colback=TipBg,
  colframe=TipBorder,
  arc=2mm,
  boxrule=0.7pt,
  left=3mm,right=3mm,top=1.5mm,bottom=1.5mm,
  title={\bfseries\color{TipText}[MẸO HAY] #1},
  coltitle=TipText,
  fontupper=\color{TipText}\small
}

\newcommand{\codeinline}[1]{\colorbox{InlineCodeBg}{\textcolor{AccentBlue}{\texttt{#1}}}}

\begin{document}

% ==================== BÌA TÀI LIỆU TIÊU CHUẨN ====================
\begin{center}
  \vspace*{1mm}
  {\fontsize{16}{20}\selectfont\bfseries\color{PrimaryNavy} HƯỚNG DẪN CÀI ĐẶT VÀ KHỞI CHẠY DỰ ÁN\\TRÊN HỆ ĐIỀU HÀNH WINDOWS\par}
  \vspace{2.5mm}
  \noindent\rule{0.6\textwidth}{0.8pt}
  \vspace{2mm}
\end{center}

\begin{tcolorbox}[colback=white,colframe=BorderBlue,arc=2mm,boxrule=0.7pt,left=3.5mm,right=3.5mm,top=2mm,bottom=2mm]
\small
\textbf{Tên dự án:} Hệ thống Đặt phòng và Quản lý Khách sạn StayGo\\
\textbf{Kiến trúc:} SPA Client-Server + Realtime Event-Driven + Polyglot Persistence\\
\textbf{Công nghệ cốt lõi:} Laravel 12, Vue 3, Node.js 22, MySQL 8.0, MongoDB 8.0, Redis 7\\
\textbf{Phương thức triển khai:} Cài đặt và khởi chạy trực tiếp trên môi trường Windows (Local Development).
\end{tcolorbox}

\vspace{1mm}

% ==================== BƯỚC 1: CHUẨN BỊ MÔI TRƯỜNG ====================
\section{BƯỚC 1: Chuẩn bị môi trường phần mềm trên Windows}

Để khởi chạy dự án trực tiếp trên Windows, máy tính của bạn cần được cài đặt sẵn các phần mềm sau:

\begin{stepbox}{Danh sách các công cụ cần có trên máy}
\begin{itemize}[leftmargin=5mm,itemsep=2pt]
  \item \textbf{PHP 8.2+ (hoặc PHP 8.3):} Bật các extension cần thiết trong file \texttt{php.ini} (\codeinline{pdo\_mysql}, \codeinline{mbstring}, \codeinline{openssl}, \codeinline{curl}, \codeinline{fileinfo}, \codeinline{mongodb}).
  \item \textbf{Composer:} Trình quản lý gói cho PHP (Tải tại: \url{https://getcomposer.org/}).
  \item \textbf{Node.js (LTS v20 hoặc v22) \& npm:} Trình thực thi JavaScript (Tải tại: \url{https://nodejs.org/}).
  \item \textbf{Cơ sở dữ liệu MySQL 8.0:} Bạn có thể dùng qua phần mềm \textbf{XAMPP}, \textbf{Laragon}, \textbf{WampServer} hoặc cài trực tiếp MySQL Community Server.
  \item \textbf{Cơ sở dữ liệu MongoDB 8.0 Community Server:} Cài đặt và bật dịch vụ MongoDB trên cổng mặc định \texttt{27017}.
  \item \textbf{Redis Server (Tùy chọn/Khuyên dùng):} Dành cho tính năng hàng đợi và broadcast thời gian thực.
\end{itemize}
\end{stepbox}

% ==================== BƯỚC 2: TẢI MÃ NGUỒN ====================
\section{BƯỚC 2: Tải mã nguồn dự án từ GitHub}

Mở cửa sổ dòng lệnh (\textbf{Command Prompt} hoặc \textbf{PowerShell}) tại thư mục bạn muốn lưu dự án (ví dụ: \texttt{D:\textbackslash} hoặc Desktop) và chạy các lệnh sau:

\begin{terminalcmd}{Tải mã nguồn từ GitHub và chuyển vào thư mục dự án}
git clone https://github.com/hongphuoc6104/quanlykhachsan.git
cd quanlykhachsan
\end{terminalcmd}

% ==================== BƯỚC 3: CẤU HÌNH VÀ CHẠY BACKEND ====================
\section{BƯỚC 3: Cài đặt và khởi chạy Máy chủ Backend (Laravel 12 API)}

\begin{stepbox}{Thực hiện các lệnh thiết lập Backend theo thứ tự}
\begin{enumerate}[leftmargin=5mm,itemsep=2pt]
  \item Di chuyển vào thư mục backend:
\begin{terminalcmd}{Chuyển vào thư mục backend}
cd backend
\end{terminalcmd}
  \item Cài đặt các thư viện PHP bằng Composer:
\begin{terminalcmd}{Cài đặt dependencies PHP}
composer install
\end{terminalcmd}
  \item Tạo file cấu hình môi trường \texttt{.env}:
\begin{terminalcmd}{Tạo file .env từ file mẫu}
copy .env.example .env
\end{terminalcmd}
  \item Mở file \texttt{.env} trong thư mục \texttt{backend} để cấu hình thông tin kết nối Cơ sở dữ liệu:
\begin{terminalcmd}{Cấu hình kết nối MySQL và MongoDB trong backend/.env}
DB\_CONNECTION=mysql
DB\_HOST=127.0.0.1
DB\_PORT=3306
DB\_DATABASE=datphong
DB\_USERNAME=root
DB\_PASSWORD=

MONGODB\_DATABASE=datphong
MONGODB\_URI=mongodb://127.0.0.1:27017/datphong
\end{terminalcmd}
  \item Khởi tạo khóa bảo mật cho ứng dụng (Application Key):
\begin{terminalcmd}{Tạo APP\_KEY}
php artisan key:generate
\end{terminalcmd}
  \item Mở phần mềm quản lý MySQL (ví dụ: \textbf{phpMyAdmin} hoặc \textbf{Navicat/HeidiSQL}) và tạo một Cơ sở dữ liệu mới có tên là \codeinline{datphong}.
  \item Chạy lệnh tạo bảng và nạp toàn bộ dữ liệu mẫu (Seeder) vào CSDL:
\begin{terminalcmd}{Chạy migration và seeder}
php artisan migrate --seed
\end{terminalcmd}
  \item Tạo liên kết thư mục lưu trữ ảnh tải lên (Storage Link):
\begin{terminalcmd}{Tạo storage symlink}
php artisan storage:link
\end{terminalcmd}
  \item Khởi chạy máy chủ Backend API:
\begin{terminalcmd}{Khởi động Laravel Server (Cổng 8000)}
php artisan serve
\end{terminalcmd}
\end{enumerate}
\end{stepbox}

% ==================== BƯỚC 4: CẤU HÌNH VÀ CHẠY REALTIME ====================
\section{BƯỚC 4: Cài đặt và khởi chạy Dịch vụ Thời gian thực (Realtime Service)}

Mở một \textbf{cửa sổ dòng lệnh mới (CMD thứ 2)}, chuyển vào thư mục \texttt{realtime} và khởi chạy:

\begin{terminalcmd}{Cài đặt và khởi chạy máy chủ WebSocket Socket.IO (Cổng 3001)}
cd realtime
npm install
npm start
\end{terminalcmd}

% ==================== BƯỚC 5: CẤU HÌNH VÀ CHẠY FRONTEND ====================
\section{BƯỚC 5: Cài đặt và khởi chạy Giao diện Web (Frontend Vue 3)}

Mở thêm một \textbf{cửa sổ dòng lệnh thứ 3 (CMD thứ 3)}, chuyển vào thư mục \texttt{frontend} và khởi chạy:

\begin{terminalcmd}{Cài đặt và khởi chạy máy chủ phát triển Frontend (Cổng 3000)}
cd frontend
npm install
npm run dev
\end{terminalcmd}

\begin{tipbox}[Tóm tắt 3 cửa sổ dòng lệnh đang chạy song song]
Để toàn bộ hệ thống hoạt động đầy đủ, bạn chỉ cần giữ 3 cửa sổ dòng lệnh:
\begin{enumerate}[itemsep=1pt]
  \item \textbf{Cửa sổ 1 (Backend):} Chạy \codeinline{php artisan serve} (Cổng 8000).
  \item \textbf{Cửa sổ 2 (Realtime):} Chạy \codeinline{npm start} (Cổng 3001).
  \item \textbf{Cửa sổ 3 (Frontend):} Chạy \codeinline{npm run dev} (Cổng 3000).
\end{enumerate}
\end{tipbox}

% ==================== BƯỚC 6: TRUY CẬP VÀ TRẢI NGHIỆM ====================
\section{BƯỚC 6: Truy cập và trải nghiệm các chức năng của hệ thống}

Mở trình duyệt web (Google Chrome, Edge, Firefox) và truy cập vào các địa chỉ sau:

\begin{table}[htbp]
  \centering
  \fontsize{9}{11.5}\selectfont
  \begin{tabularx}{\textwidth}{>{\bfseries\color{AccentBlue}}p{32mm}>{\ttfamily\color{PrimaryNavy}}p{52mm}>{\raggedright\arraybackslash}X}
    \toprule
    \rowcolor{TableHeader}
    \textcolor{white}{\textbf{Thành phần}} & \textcolor{white}{\textbf{Địa chỉ URL truy cập}} & \textcolor{white}{\textbf{Mô tả chức năng}} \\
    \midrule
    \rowcolor{TableSubHeader}
    Giao diện Web chính & http://localhost:3000 & Trang chủ đặt phòng dành cho khách hàng và đăng nhập quản trị \\
    Cổng Backend API & http://localhost:8000/api/v1 & Cổng giao tiếp REST API v1 cho toàn bộ hệ thống \\
    Kiểm tra Backend & http://localhost:8000/up & Trang theo dõi trạng thái hoạt động của Laravel \\
    Kiểm tra Realtime & http://localhost:3001/health & Trang theo dõi trạng thái kết nối WebSocket Socket.IO \\
    \bottomrule
  \end{tabularx}
\end{table}

\subsection{Danh sách tài khoản dùng thử (Đã tích hợp sẵn phân quyền RBAC)}

Hệ thống đã có sẵn 5 tài khoản mẫu tương ứng với 5 vai trò trong vận hành khách sạn. Đăng nhập tại trang \url{http://localhost:3000/login}:

\begin{table}[htbp]
  \centering
  \fontsize{8.5}{11}\selectfont
  \begin{tabularx}{\textwidth}{>{\bfseries}p{22mm}>{\ttfamily}p{44mm}>{\ttfamily}p{26mm}>{\raggedright\arraybackslash}X}
    \toprule
    \rowcolor{TableHeader}
    \textcolor{white}{\textbf{Vai trò (Role)}} & \textcolor{white}{\textbf{Email đăng nhập}} & \textcolor{white}{\textbf{Mật khẩu}} & \textcolor{white}{\textbf{Tính năng kiểm thử nổi bật}} \\
    \midrule
    \rowcolor{white}
    Super Admin & admin@gmail.com & admin123 & Toàn quyền hệ thống, quản lý tất cả khách sạn, nhân sự, cấu hình hệ thống. \\
    \rowcolor{TableSubHeader}
    Quản lý & manager@gmail.com & manager123 & Quản lý khách sạn "An Nhiên Đà Lạt", sửa giá phòng, thêm voucher, dịch vụ, xem báo cáo doanh thu. \\
    \rowcolor{white}
    Lễ tân & receptionist@gmail.com & receptionist123 & \textbf{Xem sơ đồ phòng trực quan Realtime}, xử lý Check-in/Check-out, tạo đơn tại quầy, xuất hóa đơn. \\
    \rowcolor{TableSubHeader}
    Kế toán & accountant@gmail.com & accountant123 & Quản lý danh sách hóa đơn, đối soát dòng tiền thanh toán, kiểm tra hoàn tiền khi hủy phòng. \\
    \rowcolor{white}
    Khách hàng & customer@gmail.com & customer123 & Tìm kiếm phòng theo ngày, đặt phòng, chọn voucher, thanh toán giả lập (VietQR/Card), chat CSKH realtime. \\
    \bottomrule
  \end{tabularx}
\end{table}

% ==================== BƯỚC 7: XỬ LÝ LỖI THƯỜNG GẶP ====================
\section{BƯỚC 7: Hướng dẫn xử lý các sự cố thường gặp trên Windows}

\begin{warningbox}[1. Lỗi: "could not find driver" khi chạy php artisan migrate]
\textbf{Nguyên nhân:} PHP chưa bật extension kết nối cơ sở dữ liệu MySQL.\\
\textbf{Cách xử lý:} Mở file \texttt{php.ini} (trong thư mục cài đặt PHP hoặc XAMPP), tìm dòng \texttt{;extension=pdo\_mysql} và xóa dấu chấm phẩy \texttt{;} ở đầu dòng thành \codeinline{extension=pdo\_mysql}, sau đó lưu file lại.
\end{warningbox}

\begin{warningbox}[2. Lỗi: "Class 'MongoDB\textbackslash Driver\textbackslash Manager' not found"]
\textbf{Nguyên nhân:} PHP chưa cài đặt extension MongoDB trên Windows.\\
\textbf{Cách xử lý:} Tải file \texttt{php\_mongodb.dll} tương ứng với phiên bản PHP từ PECL (\url{https://pecl.php.net/package/mongodb}), chép vào thư mục \texttt{php/ext/}, sau đó thêm dòng \codeinline{extension=mongodb} vào file \texttt{php.ini}.
\end{warningbox}

\begin{warningbox}[3. Lỗi: "Access denied for user 'root'@'localhost'"]
\textbf{Nguyên nhân:} Sai thông tin tài khoản hoặc mật khẩu kết nối MySQL.\\
\textbf{Cách xử lý:} Mở file \texttt{backend/.env}, kiểm tra lại các thông số \codeinline{DB\_USERNAME} và \codeinline{DB\_PASSWORD} cho trùng khớp với tài khoản MySQL trên máy tính của bạn.
\end{warningbox}

\begin{warningbox}[4. Lỗi: Không tải được dữ liệu phòng hoặc báo lỗi mạng ở Frontend]
\textbf{Nguyên nhân:} Máy chủ Backend chưa được bật hoặc chưa chạy lệnh seeder dữ liệu.\\
\textbf{Cách xử lý:} Đảm bảo cửa sổ dòng lệnh Backend đang chạy lệnh \codeinline{php artisan serve} và đã chạy thành công lệnh \codeinline{php artisan migrate --seed}.
\end{warningbox}

\vspace{3mm}
\begin{center}
  \noindent\rule{0.6\textwidth}{0.8pt}\\
  \vspace{1.5mm}
  {\small\color{gray}Chúc bạn cài đặt thành công và có trải nghiệm tuyệt vời với hệ thống StayGo!}
\end{center}

\end{document}
